import 'package:flutter/material.dart';

// --- Paddings --- //

/// Padding for the "New" badge, with specific left, top, right, and bottom values.
const EdgeInsets kGameBadgeNewPadding = EdgeInsets.fromLTRB(5, 4, 5, 0);

// --- Paddings --- //

/// Horizontal padding for game badges.
const double kGameBadgeHorizontalPadding = 3;

/// Vertical padding for the "Play" badge.
const double kGameBadgePlayVerticalPadding = 16;

/// Horizontal padding for the "Play" badge.
const double kGameBadgePlayHorizontalPadding = 31;

// --- Sizes --- //

/// Default size for game badges.
const double kGameBadgeDefaultSize = 25;

/// Size for the Illiko badge image.
const double kGameBadgeIllikoSize = 57;

// --- Colors --- //

/// Text color for game badges.
const Color kGameBadgeTextColor = Color(0xff292d82);

/// Background color for the "New" badge.
const Color kGameBadgeNewBackgroundColor = Color(0xffec6c40);

// --- Radii --- //

/// Border radius for the "New" badge.
const double kGameBadgeNewBorderRadius = 3;

/// Radius for game badges.
const double kGameBadgeRadius = 90;

// --- Font Sizes --- //

/// Font size for text on the "New" badge.
const double kGameBadgeNewFontSize = 8;

/// Font size for text on the "Play" badge.
const double kGameBadgePlayFontSize = 11;

// --- Sources --- //

/// Path to the image for the purple Illiko badge.
const String kGameBadgeIllikoPurpleImagePath =
    'assets/images/home/illikoBadges/illiko_purple_badge.png';

/// Path to the image for the blue Illiko badge.
const String kGameBadgeIllikoBlueImagePath =
    'assets/images/home/illikoBadges/illiko_blue_badge.png';
