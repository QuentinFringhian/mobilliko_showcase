// --- Paddings --- //

/// Top padding for the game carousel card.
const double kGameCarouselCardTopPadding = 20;

/// Bottom padding for the game carousel card.
const double kGameCarouselCardBottomPadding = 30;

/// Padding between the main elements of the game carousel card.
const double kGameCarouselCardMainSeparationPadding = 16;

/// Padding between buttons in the game carousel card.
const double kGameCarouselCardButtonsSeparationPadding = 20;

/// Padding between badges in the game carousel card.
const double kGameCarouselCardBadgesSeparationPadding = 15;

/// Right padding for the "New" section in the game carousel card.
const double kGameCarouselCardNewRightPadding = 15;

// --- Sizes --- //

/// Size of the game logo in the carousel card.
const double kGameCarouselCardLogoSize = 94;

/// Size of the favorite icon in the carousel card.
const double kGameCarouselCardFavoriteSize = 42;

/// Size of the Illiko badge in the carousel card.
const double kGameCarouselCardIllikoSize = 80;

/// Size of the price badge in the carousel card.
const double kGameCarouselCardPriceSize = 30;

// --- Font Sizes --- //

/// Font size for the maximum gain text in the carousel card.
const double kGameCarouselCardMaxGainFontSize = 21;

/// Font size for the "New" badge text in the carousel card.
const double kGameCarouselCardNewFontSize = 10;
